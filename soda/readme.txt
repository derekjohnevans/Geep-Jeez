Soda is a new component model im working on. It extends the standard TComponent,
and provides features like type safe owners, items, searches, etc.

Soda takes care of all the nitty gritty component type protection code which
is often not implemented or, buggy.
